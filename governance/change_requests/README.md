# Change Requests

This directory mirrors the Change Request (CR) database in Notion.

Workflow:
1. Draft in Notion (Status: Draft)
2. Create a CR entry (CR-YYYYMMDD-###)
3. Implement changes via Pull Request referencing the CR ID
4. Merge to main and mark Notion page as Approved
